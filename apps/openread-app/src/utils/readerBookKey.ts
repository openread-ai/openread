export {
  createReaderBookKey,
  getBookRefFromReaderBookKey,
  isReaderBookKeyOrRef,
  normalizeBookReference,
  parseBookRefFromReaderBookKey,
} from '@openread/types';
export type { OpenReadBookReference, ReaderBookKey } from '@openread/types';

export { createReaderBookKey as createBookKey } from '@openread/types';
export { getBookRefFromReaderBookKey as getBookIdFromKey } from '@openread/types';
